// import { render, screen } from "@testing-library/react";
// import App from "./App";
// //test(name , fn , timeout)
// //name for identify test
// // function that contain what we want to test
// // timeout is optional and use before aborting the test and default value is 5 sec

// test("renders learn react link", () => {
//   //app component render
//   render(<App />);
//   // screen is object to query on virtual dom
//   //for example here we use  getByText
//   //since situation meets , we pass the test
//   // after we go through expect part and check
//   //if the thing we want is in a component we rendered
//   const linkElement = screen.getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// });

// //test and expect is not imported it is globaly
// //imported by jest
